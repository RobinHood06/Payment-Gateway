server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/financial_gateway
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
